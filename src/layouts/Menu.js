import React from 'react';
import Navigation from '../components/Navigation';

const Menu = () => {
  return (
    <div className='menu'>
      {<Navigation />}
    </div>
  );
}

export default Menu;